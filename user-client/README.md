# Nepali News Portal User Client

![Home page screenshot](./public/screenshot-homepage.png)

## Getting Started

First, install the dependencies:

```bash
yarn
```

Then, run the server:

```bash
yarn build && yarn start
```

Open <http://localhost:3000> with your browser to see the result.
